.form_guess_container
  width 80%
  max-width 15rem
  margin auto
  padding 1rem
  input[type=text]
    width 100%
    padding 0.6rem 1rem
    border 1px solid #cccccc
    margin-bottom 0.5rem
    resize: vertical
    &:focus
      border-color $icon-color
